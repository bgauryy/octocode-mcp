import{findImportLine as e}from"./shared.js";import{canAddFinding as t}from"./shared.js";import{isTestFile as i}from"../common/utils.js";export function computeInstability(e,t){const i=e+t;return 0===i?0:t/i}export function detectSdpViolations(t,n=.15,o=.6){const s=[],r=new Map,a=e=>{if(r.has(e))return r.get(e);const i=computeInstability((t.incoming.get(e)||new Set).size,(t.outgoing.get(e)||new Set).size);return r.set(e,i),i};for(const r of t.files){if(i(r))continue;const c=t.outgoing.get(r)||new Set,l=a(r);for(const d of c){if(!t.files.has(d)||i(d))continue;const c=a(d),u=c-l;if(u>n&&l<o){const i=e(t,r,d);s.push({severity:u>.3?"high":"medium",category:"architecture-sdp-violation",file:r,lineStart:i.lineStart,lineEnd:i.lineEnd,title:"SDP violation: stable module depends on unstable module",reason:`"${r}" (I=${l.toFixed(2)}) depends on "${d}" (I=${c.toFixed(2)}). Delta=${u.toFixed(2)}.`,files:[r,d],suggestedFix:{strategy:"Invert dependency via interface/abstraction or move shared code to a stable utility.",steps:["Extract a stable interface that the stable module depends on.","Have the unstable module implement that interface.","Consider moving shared logic to a lower-instability utility module."]},impact:"Prevents cascading instability and reduces change propagation risk.",tags:["stability","coupling","architecture","sdp"]})}}}return s}export function detectHighCoupling(e,t=15){const n=[];for(const o of e.files){if(i(o))continue;const s=(e.incoming.get(o)||new Set).size,r=(e.outgoing.get(o)||new Set).size,a=s+r;a>t&&n.push({severity:a>25?"high":"medium",category:"high-coupling",file:o,lineStart:1,lineEnd:1,title:`High coupling: ${o}`,reason:`Module has ${a} total connections (Ca=${s}, Ce=${r}). Threshold: ${t}.`,files:[o],suggestedFix:{strategy:"Reduce coupling by extracting interfaces or splitting module responsibilities.",steps:["Identify groups of related imports/dependents that can be isolated.","Extract focused sub-modules with single responsibilities.","Use dependency inversion to reduce direct coupling."]},impact:"Lower coupling reduces change ripple effects and improves testability.",tags:["coupling","change-risk","architecture"]})}return n}export function detectGodModuleCoupling(e,t=20,n=15){const o=[];for(const s of e.files){if(i(s))continue;const r=(e.incoming.get(s)||new Set).size,a=(e.outgoing.get(s)||new Set).size;r>t&&o.push({severity:r>1.5*t?"high":"medium",category:"god-module-coupling",file:s,lineStart:1,lineEnd:1,title:`High fan-in bottleneck: ${s}`,reason:`Module is depended on by ${r} modules (threshold: ${t}). Changes ripple widely.`,files:[s],suggestedFix:{strategy:"Split this module into focused sub-modules to reduce blast radius.",steps:["Identify distinct groups of consumers using different parts of this module.","Extract each group into a dedicated module.","Update import paths incrementally."]},impact:"Reduces change blast radius and improves parallel development.",tags:["coupling","blast-radius","bottleneck"]}),a>n&&o.push({severity:a>1.5*n?"high":"medium",category:"god-module-coupling",file:s,lineStart:1,lineEnd:1,title:`High fan-out: ${s}`,reason:`Module depends on ${a} modules (threshold: ${n}). It may violate single responsibility.`,files:[s],suggestedFix:{strategy:"Reduce dependencies by introducing facade or mediator patterns.",steps:["Group related imports behind a single facade module.","Consider splitting this module by responsibility.","Use dependency injection to reduce direct coupling."]},impact:"Cleaner architecture and easier testing through reduced dependencies.",tags:["coupling","responsibility","sprawl"]})}return o}export function detectLayerViolations(t,n){if(n.length<2)return[];const o=[],s=e=>{for(let t=0;t<n.length;t++)if(e.includes(n[t]))return t;return-1};for(const r of t.files){if(i(r))continue;const a=s(r);if(-1!==a)for(const c of t.outgoing.get(r)||new Set){if(!t.files.has(c)||i(c))continue;const l=s(c);if(-1!==l&&l<a){const i=e(t,r,c);o.push({severity:"high",category:"layer-violation",file:r,lineStart:i.lineStart,lineEnd:i.lineEnd,title:`Layer violation: ${n[a]} imports from ${n[l]}`,reason:`"${r}" (layer: ${n[a]}) imports "${c}" (layer: ${n[l]}). Layer order: ${n.join(" → ")}.`,files:[r,c],suggestedFix:{strategy:"Respect layer boundaries by inverting the dependency or moving shared logic.",steps:["Extract shared contracts to a lower layer that both can depend on.","Use dependency inversion: define an interface in the lower layer, implement in higher.","If the dependency is justified, reconsider your layer boundaries."]},impact:"Prevents architectural erosion and keeps dependency flow unidirectional.",tags:["architecture","layering","coupling"]})}}}return o}export function computeAbstractness(e){if(0===e.length)return 0;return e.filter(e=>"type"===e.kind).length/e.length}export function detectDistanceFromMainSequence(e,n=.7,o=3){const s=[];for(const r of e.files){if(i(r))continue;const a=e.declaredExportsByFile.get(r);if(!a||0===a.length)continue;const c=(e.incoming.get(r)||new Set).size,l=(e.outgoing.get(r)||new Set).size;if(c+l<o)continue;const d=computeInstability(c,l),u=computeAbstractness(a),g=Math.abs(u+d-1);if(g<n)continue;const p=u<.2&&d<.3,f=u>.7&&d>.7;let h="";if(h=p?"Zone of Pain (concrete + stable): hard to extend, painful to change.":f?"Zone of Uselessness (abstract + unstable): over-abstracted and unused.":"Far from Main Sequence: balance between abstraction and stability is off.",!t(s))break;s.push({severity:g>.85?"high":"medium",category:"distance-from-main-sequence",file:r,lineStart:1,lineEnd:1,title:`Distance from Main Sequence: ${r} (D=${g.toFixed(2)})`,reason:`${h} A=${u.toFixed(2)}, I=${d.toFixed(2)}, D=${g.toFixed(2)} (threshold: ${n}).`,files:[r],suggestedFix:{strategy:p?"Add abstractions (interfaces/types) or reduce inbound coupling.":f?"Add concrete implementations or remove unused abstractions.":"Rebalance by adjusting abstraction level or dependency direction.",steps:p?["Extract interfaces for key behaviors to increase abstractness.","Consider splitting into abstract contracts + concrete implementations.","Reduce inbound coupling by narrowing the public API surface."]:f?["Verify abstractions have concrete implementations.","Remove unused interfaces/types that serve no consumer.","Consider consolidating with concrete modules."]:["Review the balance between interfaces/types and concrete exports.","Adjust dependency direction to move closer to the Main Sequence.","Consider splitting responsibilities between abstract and concrete modules."]},impact:"Modules on the Main Sequence (D≈0) have optimal balance between stability and extensibility.",tags:["architecture","stability","abstractness","sdp"]})}return s}