# Deferred autoresearch ideas

- Add static direct-render help coverage for the remaining auth-family commands (`auth`, `login`, `logout`, `status`) if the widened benchmark confirms they still load the heavy command/help path.
