# Overview

## Identification
- **File**: `node_modules/@anthropic-ai/claude-agent-sdk/sdk.mjs`
- **Version**: 0.1.76
- **Type**: Minified/Bundled JavaScript (Node.js executable)
- **Bundler**: Likely esbuild (based on `__create`, `__commonJS` helpers)

## Structure
- Starts with shebang `#!/usr/bin/env node`
- Contains standard bundling boilerplate (`__create`, `__toESM`, `__commonJS`)
- Appears to be the main SDK entry point

## Analysis Status
- Initial read successful via `head`.
- `local_ripgrep` failing to find matches (possibly tool config issue with node_modules).
- Switching to `grep` for pattern discovery.
