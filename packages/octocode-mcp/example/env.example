# OAuth-Enabled MCP Server Configuration
# ==========================================

# GitHub OAuth App Configuration
# Create your OAuth App at: https://github.com/settings/applications/new

# Required: Your GitHub OAuth App credentials
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Optional: GitHub Enterprise Server (leave blank for github.com)
# GITHUB_HOST=https://github.enterprise.com

# Optional: Custom port (default: 3000)
# PORT=3000

# OAuth App URLs (configure these in your GitHub OAuth App):
# Homepage URL: http://localhost:3000
# Authorization callback URL: http://localhost:3000/api/auth/callback/github

# ==========================================
# MCP Client Integration
# ==========================================

# For Octocode MCP Server with OAuth:
# Add these environment variables to your MCP client configuration:

# GITHUB_OAUTH_CLIENT_ID=your_github_client_id_here
# GITHUB_OAUTH_CLIENT_SECRET=your_github_client_secret_here  
# GITHUB_OAUTH_REDIRECT_URI=http://localhost:3000/api/auth/callback/github
# GITHUB_OAUTH_SCOPES=repo,read:user,read:org
# GITHUB_OAUTH_ENABLED=true
# MCP_SERVER_RESOURCE_URI=http://localhost:3000

# Example Claude Desktop configuration:
# {
#   "mcpServers": {
#     "octocode-with-oauth": {
#       "command": "npx",
#       "args": ["octocode-mcp"],
#       "env": {
#         "GITHUB_OAUTH_CLIENT_ID": "your_client_id",
#         "GITHUB_OAUTH_CLIENT_SECRET": "your_client_secret",
#         "GITHUB_OAUTH_REDIRECT_URI": "http://localhost:3000/api/auth/callback/github",
#         "GITHUB_OAUTH_SCOPES": "repo,read:user,read:org",
#         "GITHUB_OAUTH_ENABLED": "true",
#         "MCP_SERVER_RESOURCE_URI": "http://localhost:3000"
#       }
#     }
#   }
# }

# For testing this local OAuth endpoint directly:
# {
#   "mcpServers": {
#     "local-oauth-demo": {
#       "command": "node",
#       "args": ["http://localhost:3000/mcp"],
#       "transport": {
#         "type": "http"
#       }
#     }
#   }
# }
